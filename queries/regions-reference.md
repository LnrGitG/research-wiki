---
title: "Канонический справочник регионов research-wiki"
created: 2026-09-16
updated: 2026-09-16
type: query
tags: [infrastructure, database, reference-data, regions, harmonization]
status: completed
related: ["queries/database-architecture", "queries/database-target-spec", "queries/db-cleanup-report"]
---

# Канонический справочник регионов

Закрывает вопрос 14.8 из [[queries/database-architecture]] — последний
незакрытый вопрос архитектуры единой базы данных.

## Задача

В шести существующих SQLite-базах имена географических сущностей хранились как
свободный текст. Проверка выявила **646 уникальных написаний**, из которых:

- часть — реальные гео-сущности в разных формах (латиница ФНС, аббревиатуры,
  родительный падеж, сноски, омоглифы);
- часть — исторические сущности (автономные округа, упразднённые в 2005–2008);
- часть — вовсе не география, а подписи строк, попавшие в колонку «регион»
  из-за смещения колонок при парсинге широких XLSX.

## Решение

Построен канонический справочник **102 записей** с иерархией:

| Уровень | Количество | Пример |
|---|---|---|
| `country` | 1 | Российская Федерация |
| `federal_district` | 8 | Центральный федеральный округ |
| `region` | 91 | 89 субъектов РФ + 2 агрегата «с автономными округами» |
| `aggregate` | 1 | Новые территории (свод ДОМ.РФ) |
| `city` | 1 | Сочи |

Колонка `aggregation` различает базовые ряды (`base`) и агрегаты
(`with_ao_included`, `sum_of_new_territories`).

Каждая запись несёт `region_code` (латиница, машинный ключ), `name` (каноническое
русское название), `level`, `okato` (где применим) и `parent_code` — ссылку на
родительский федеральный округ, что позволяет корректно агрегировать данные
от региона к округу и к стране.

## Метод сопоставления

Четыре прохода, применённые последовательно:

1. **Точные алиасы** (227 написаний) — явная таблица для исторических сущностей,
   английских слогов ФНС (`moscow city`, `tatarstan`), двусмысленных случаев.
2. **Нормализация** (38) — латинские омоглифы в кириллице (`Аpхангельская` →
   Архангельская), раскрытие аббревиатур (`АО`, `авт.округ`), снятие сносок
   (`2)`, `4`), служебные префиксы (`г.`, `в том числе`), родительный падеж
   (`Забайкальского края` → Забайкальский край).
3. **Прямое совпадение с каноном** (144) — стандартные написания.
4. **Нечёткое сравнение** (26) с порогом 0.90 — опечатки источника
   (`Северо-Каваказского` → Северо-Кавказского).

## Покрытие

- **89 из 89 регионов** получили хотя бы один алиас — пропусков нет.
- **2 354 514 наблюдений** покрыто картой алиасов.
- **Ноль** несопоставленных гео-сущностей.

## Не-региональные значения

211 значений исключены из справочника как не-географические. Это подписи строк,
попавшие в колонку региона при парсинге широких таблиц Росстата: объёмы
(`Введено всего, тыс. кв. м`), стадии продаж (`Продано`, `Продажи не открыты`),
уровни готовности, виды объектов (`жилого назначения`, `нежилого назначения`),
категории материалов (`цемента, тыс. т`), годы в колонке региона (`2020.0`),
кварталы (`I квартал`) и группы городов по размеру.

Отдельно отмечу: **`жилого назначения` / `нежилого назначения`** — это не мусор,
а легитимный подразрез показателя. Именно он обосновал добавление колонки
`sub_dimension` в ключ наблюдения (решение 14.5 архитектуры). Такие значения
должны попасть в поле подразреза, а не в справочник регионов.

## Спецслучаи: разобраны 16.09.2026

### «Малороссийский ФО» → свод новых территорий (115 наблюдений)

Проверка исходного файла `raw/domrf/01_05_stockvariables_sales.xlsx` (лист
«3 Регионы реализация метры», строка 380) показала: строка идёт **между**
Чукотским АО и четырьмя новыми территориями — ДНР, ЛНР, Херсонской,
Запорожской. Значения не пустые: за август 2026 г. реализация квартир
1 395 440 м² (для сравнения ДНР — 514 670, Чукотка — 14 098). Это не опечатка
и не артефакт, а **сводный агрегат новых территорий**, который ДОМ.РФ
публикует отдельной строкой.

Решение: включён в справочник как `ntd` «Новые территории (свод)» уровня
`aggregate`; ДНР, ЛНР, Запорожская и Херсонская области привязаны к нему
родителем вместо Южного ФО.

### «Сочи» → уровень city

Единственное наблюдение (`vvod_jil_dom_RF_2025.xls`, показатель «Доля ввода
жилья по стандартам экономкласса», 2014 г.) — остаток старого формата
публикации Росстата, где рядом с регионами перечислялись крупные города.
Вынесен отдельным уровнем `city`, в агрегаты не входит.

### Составные названия Росреестра (387 строк) → склейка заголовков

Проверка `housing_input_annual`: длинное тире здесь разделяет **уровни
заголовков таблицы**, а не части названия региона. Пример сырого значения:
«Всего введено в действие жилых домов — Населением за счет собственных и
привлеченных средств — Всего, млн. м2». Это три уровня иерархии, склеенные
парсером. Настоящее составное название региона здесь одно —
«Архангельская область — Ненецкий автономный округ» (21 наблюдение).

Решение: 387 строк классифицированы как `row_label_stacked`; при миграции их
следует расщепить на три колонки (категория, источник средств, показатель),
а не хранить как название региона.

### Ошибка, найденная при разборе: «с АО» и «без АО» были сведены в один код

Исходная версия справочника срезала скобочные уточнения целиком, из-за чего
«Архангельская область (с автономным округом)» и «(без автономного округа)»
попадали в один код `arkhangelsk`. Но это **разные величины**, а не варианты
написания. Исправлено:

| Вариант | Наблюдений | Код | Признак |
|---|---|---|---|
| Архангельская область (с автономным округом) | 3 218 | `arkhangelsk_nao` | `with_ao_included` |
| Архангельская область (без автономного округа) | 3 185 | `arkhangelsk` | `base` |
| Тюменская область (с автономными округами) | 3 221 | `tyumen_ao` | `with_ao_included` |
| Тюменская область (без автономных округов) | 3 257 | `tyumen` | `base` |

Канонично по методологии Росстата — регион **без** автономных округов;
округа учитываются отдельно. Вариант «с округом» — агрегат, помеченный
признаком агрегации.

### Оставшиеся на ревью

- `baikonur` (37 наблюдений) — арендуется у Казахстана, статус особый,
  в справочник субъектов РФ не включён.

## Артефакты

- `data/regions_canonical.csv` — справочник 102 записей
- `data/region_aliases.csv` — карта 432 сырых написаний → канонический код
- `data/region_unmatched.txt` — 214 не-региональных значений (подписи строк)
- `scripts/build_region_reference.py` — воспроизводимый построитель

Скрипт идемпотентен: повторный запуск пересоздаёт артефакты из баз без
изменения результата.

## Вывод для research-wiki

Справочник закрывает предпосылку версионного слоя: без единого ключа региона
невозможно ни сопоставить оценки разных источников (аналитический режим —
показатель Росстата по Архангельской области без НАО против оценки ДОМ.РФ),
ни построить длинные ряды (регион переживал переименования и изменения состава).
Теперь `region_id` становится первичным полем наблюдения, а текстовое название
уходит в справочник алиасов как исторический факт.
